services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: midia-downloader-bot:latest
    container_name: midia-downloader-bot
    cpus: 1.5
    mem_limit: 1g
    pids_limit: 256
    env_file:
      - .env
    restart: unless-stopped
